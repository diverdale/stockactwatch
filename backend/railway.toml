[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head; echo \"ALEMBIC_EXIT=$?\"; echo \"PORT=$PORT\"; python -c \"import app.main; print('IMPORT_OK')\" 2>&1; python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} 2>&1"
restartPolicyType = "always"
