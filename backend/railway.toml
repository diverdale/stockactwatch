[build]
builder = "nixpacks"

[deploy]
startCommand = "alembic upgrade head; echo \"ALEMBIC_EXIT=$?\" >&2; echo \"PORT=$PORT\" >&2; python -c \"import app.main; print('IMPORT_OK')\" >&2 2>&1; python -m uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
restartPolicyType = "always"
